import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.task.completer.ui',
  appName: 'ionic.task-completer-ui',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
